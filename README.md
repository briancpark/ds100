# DS 100
Data Science 100: Principles and Techniques of Data Science

## Setup
If Anaconda is already installed, then all that needs to be executed is this:
```sh
conda env create -f data100_environment.yml
```

In addition, for code to run properly otter-grader should be installed:
```sh
pip3 install otter-grader
```